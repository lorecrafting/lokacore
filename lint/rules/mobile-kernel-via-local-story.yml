id: mobile-kernel-via-local-story
language: tsx
severity: error
message: Only mobile/authority/local-story imports the kernel (spec document 10 §2).
files: [mobile/**]
ignores: [mobile/authority/local-story/**]
rule:
  kind: string_fragment
  regex: '(^|/)kernel(/|$)'
  inside: { matches: module-specifier }
