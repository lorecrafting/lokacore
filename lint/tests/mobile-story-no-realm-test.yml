id: mobile-story-no-realm
valid:
  - "import { KERNEL_ID } from '../../../kernel/ts/src/index.ts';"
  - "const s = 'authority/local-story kernel/ts features/realm features/story';"
  - "export const label = 'realm/remote-realm/story/kernel/authority';"
  - "jest.mock('../../packages/ui', () => ({ label: 'story realm kernel authority' }));"
  - "const m = jest.fn('../realm');"
invalid:
  - "export { y } from '../../authority/remote-realm';"
  - "import { a } from '../realm';"
  - "import { b } from '../remote-realm/index';"
  - "const load = () => import('../realm');"
  - "const r = require('../../authority/remote-realm');"
  - "const t = require(`../realm`);"
  - "jest.mock('../realm');"
  - "jest.mock('../realm', () => ({}));"
  - "const a = jest.requireActual('../realm');"
  - "const p = require.resolve('../realm');"
