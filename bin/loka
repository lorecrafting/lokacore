#!/bin/sh
# Loka's command line. `loka play <artifact> [script | --replay <transcript>]`: kernel/ts/play.
[ "${1-}" = play ] || { echo "usage: bin/loka play <artifact> [script | --replay <transcript>]" >&2; exit 2; }
shift
exec node "$(dirname "$0")/../kernel/ts/play/main.ts" "$@"
