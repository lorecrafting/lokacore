{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.ex|*.exs|*.heex) cd \"$(git -C \"$(dirname \"$f\")\" rev-parse --show-toplevel)\" && mise exec -- mix format \"$f\" ;; esac"
          }
        ]
      }
    ]
  }
}
