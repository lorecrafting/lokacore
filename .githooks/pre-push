#!/bin/sh
# The full check line; kernel/ts only when it differs from upstream (or there is none).
if git diff --quiet '@{upstream}' HEAD -- kernel/ts 2>/dev/null; then
  exec bin/check_all.sh --no-kernel
fi
exec bin/check_all.sh
