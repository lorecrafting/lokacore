# Review: R5 S6b resources (HP/MA/MV), costs, threshold checks, cooldowns, 1 MV per move, regeneration (PR #49)

- PR: [#49](https://github.com/lorecrafting/lokacore/pull/49), branch `r5-s6b-resources`.
- Commit reviewed: `7cb4af213a7c72908e291e5bee0495337f5213cb` (CI green: bundle, elixir, lint,
  typescript).
- Reviewer: a fresh Claude Code agent (Opus) that authored none of the work. Fable reviews are
  suspended, so the frozen surface (the two new delta ops in both kernels, the new schema
  shapes, the event-stream and lock decisions) was judged against the spec, not only the code
  against the brief. An Astra review runs in parallel; the PM merges its findings.
- Verdict: **APPROVE WITH NOTES** (two should-fix, three nits).
- Read first: the S6b brief, plus the PM's later binding rulings: every v2 cartridge gets
  `resource@1` and the pools (owner decision HP/MA/MV, end; 00 §4 amendment on main); the
  target is the body; regeneration lives in compose as `{value, at}` with hour ticks; 32-bit
  bounds; a cooldown rejection carries no remaining time. Also 21 §4 Resource and Time, §7
  ActionRecipe, Check and Cost; 06 §20; 04 §5.0-§5.3 (the resource adjustment row), §7; 03 §14;
  02 §A derived state; 00 §4 amendment and rows; 14 §R5; the S5 and S6a records (Astra: parity
  at the install boundary, no throw escaping `step`, event streams frozen per capability
  version); the contract lessons.

## What must be true (from the spec, before the diff)

1. `resource.adjust` is checked integer arithmetic in an explicit sequence. Every step checks
   `from` against the current value and `to` against the bounds, and never clamps. Writers in
   different groups on one resource conflict (04 §5.3 row).
2. The current value is derived from a stored value and time (02 §A), with regeneration
   stopping at the maximum. That stop belongs to regeneration, not to an adjustment. A write
   stores the derived value. Both kernels compute the same thing, including at hour boundaries.
3. An unaffordable cost or an active cooldown is a rejection. It draws no RNG, moves no time,
   pays nothing and changes nothing (04 §5.0). An admitted failed attempt pays its costs and
   starts its cooldown, like a success (04 §5.0).
4. The compiler and the loader accept and reject the same artifacts for every new field. No
   schema-valid artifact makes `step` throw (S6a Astra).
5. Event streams: `resource@1` emits nothing. `movement@1`'s and `check@1`'s events are
   unchanged for every existing lock. `movement@1`'s delta changes only under a new lock
   combination (#44 F2).
6. Formats are additive (tag `loka-cartridge-v2`). Changed known answers and transcripts are
   exactly the ones the PM ruling allows, and each is re-derived independently of the kernels.
   `ashmere_hello` (v1) keeps its bytes, and an artifact without `resource@1` loads with no
   pools.
7. Pools belong to the actor's body (`bodyOf`), not to `world.character` (puppeting).

## Checks performed

- Local suites at `7cb4af2` in a detached worktree: `npm test` 173/173, `mix test` 190/190.
- **Known answers re-derived independently.** For each of bell, details, dusk, facts, items and
  rooms I took the `origin/main` value and added `resource: 1` to requires and the lock. I added
  the three pools with the owner decision's numbers (hp 0..20/20/5, ma 0..100/100/4, mv
  0..82/82/18). I canonicalised with Python `json.dumps(sort_keys, compact, ensure_ascii=False)`
  and hashed with hashlib. All six sha256 values, `canonical` strings and values match the PR's.
  For `cartridge_road_hash.json`, the `canonical` string is the canonical form of `value`, the
  sha256 matches, and the resources match `resources.json` merged over the defaults.
- **Changed transcripts classified field by field** against `origin/main`. Every line kept its
  keys. The only changed fields are `ids.content_hash`, `ids.kernel_version` and
  `data.decision.delta_digest`, and the digest changes only on `move` commands. Events are
  byte-identical, so `movement@1`'s event stream is unchanged. All seven transcripts (six
  changed, one new) replay with `loka play --replay` against freshly compiled artifacts:
  "N commands identical".
- `git diff --diff-algorithm=histogram --numstat` shows `composition.json` +1019/-0 and
  `invalid.json` +856/-0. All 467 old invalid cases are present unchanged, with 25 added.
  `ashmere_hello` and its fixture are untouched.
- **Hand checks of the `pools` composition state** (clock 7300): mv 10 stored at 3500 crosses
  two boundaries (floor 2 - floor 0), so 10 + 36 = 46. Hours since the write
  would give 28 (the negative case). hp 18 at 0 gives 28, stopped at 20. ma unset (start 50)
  gives 58. mv unset at its maximum stays 82. All match the fixture.
- **Cross-kernel probe** (throwaway scripts running both `compose` on the same inputs):
  int32 extremes (gain 2^31-1 over a clock of 2^53-1 stops at 2^31-1; `to` of max+1 and min-1
  fault), stored at 3599 with clock 3600 (one tick), at 3600 with clock 7199 (none), `at ==
  clock`, a same-group chain after regeneration, `time.advance` then `resource.adjust` in one
  group (the base clock is used, not the advanced one), a stored value above the maximum, `at`
  after the clock, and a cooldown restart chain. Byte-identical on every valid state. They
  differ only on states no path can produce (N1).
- TypeScript mutants, each killed (failures in brackets): threshold `>` for `>=` (2); threshold
  read after costs (2); `cooldown.start` without `from` (3); `adopt` dropping the resource
  section (7); the invariant reading the stored instead of the derived value (2); compose
  without the minimum bound (1); `pay` never refusing (4); a move never charging (5); costs
  missing from the delta (5); a step clamping instead of faulting (5); a failed attempt not
  paying (3); a failed attempt not starting the cooldown (1); the loader's threshold, step and
  lock resource checks removed (1 each); compose not checking the cooldown's `at` (1); cooldown
  one second short (1). Three no-op controls stayed green.
- Elixir mutants (`mix test --force`), each killed: no stop at the maximum (1); regeneration
  counted since the write (2); no bounds (2); the cooldown's `at` unchecked (2); the invariant on
  the stored value (3); no RESOURCE_SPEC_INVALID (1); the threshold resource unchecked (1); no
  `resource@1` added (11).
- The differential draws both ops from the `base` and `pools` states, with clocks 3599, 3600 and
  7300 among the picks (`compose_test.exs`).
- Items 3 and 7: `admit` (cooldown, then costs) runs before `resolve`, and a rejection returns
  the input world. The author's "RNG drawn before the cost rejection" is truly an equivalent
  mutant. `movement` and `action_recipe` take the body from `bodyOf(world, actor_id)`.
- Item 5: the registry's `resource` entry owns only the `resource` definition kind: no command,
  no event. A threshold emits the existing `check_passed`/`check_failed`. A cartridge's pools
  need `resource@1` in its lock (the loader's lockStage, and the compiler through
  `Resources.requires`), so the move charge exists only under the new lock combination. Before
  this PR no artifact could lock `resource@1` (not INSTALLED). Sound under #44 F2.
- Emergence: the composes-with statement holds. `resource.ts` names no content. `movement`
  names `mv`, which 00 §4 requires. No capability names another mechanic.
- Over-engineering: nothing to cut. `view.ts` and the `choice` split exist for the size limit.
  The duplicated luck branch in `ActionRecipe.check` is the stated price of not changing
  `RecipeCheck`'s frozen invalid cases. No size or credo escape hatches were added.

## Findings

**S1 (should-fix): the approved "a step leaving bounds is a fault" rule makes ordinary healing
fault, and no author can prevent it.** `kernel/ts/src/resource.ts:46` computes `to = from + by`,
and compose faults when `to > maximum` (the intended "never clamp"). Recipes cannot read
resources: no policy leaf does, and 06 §21's `resource_compare` is not built. So a recipe with
a positive `resource.adjust` step has no way to be unavailable at full value. Failure (run with
`loka play` on the demo, `cartridges/ashmere_road/recipes/pray.json`):
`e, pray, wait, pray, wait, pray` ends in `(fault: precondition_failed)` at `hp 25/25`, in six
ordinary commands. `resources.test.ts` pins this as intended. However, 04 §5.2 step 7 treats a
fault as a discarded evaluation (budget, conflict, evaluator), not a game outcome. A player who
prays at full health should get a refusal or a successful no-op, not a fault. The behaviour is
correct to the approved design, so this is for the PM to decide. Cheapest spec-native options:
(a) add the `resource_compare` policy leaf (06 §21), so authors can gate a heal and the action
shows unavailable; (b) reject a perform `invalid_state` at admission when a step would leave
bounds, for recipes without a luck check (after a draw a rejection is not allowed, 04 §5.0);
(c) keep the rule, record it as a known limit, and change the demo so that it does not ship a
recipe that faults in normal play. Whichever the PM picks, R7's healing and potions will need
it.

**S2 (should-fix): a schema-valid long cooldown turns the cooldown refusal into
`evaluator_error`.** `kernel/ts/src/rules/action_recipe.ts:99` computes
`add(last, recipe.cooldown)`, and `ActionRecipe.cooldown` (`protocol/action.schema.json:752`)
has `minimum: 1` and no maximum. Failure (probed): the demo with `"cooldown":
9007199254740991` (an author's "once ever"), then `e, wait, pray, pray`. The second pray, with
`last = 3600`, overflows 2^53-1. `decideWith` catches the KernelError, so the player gets
`(fault: evaluator_error)` instead of the `cooldown` rejection. It is contained (nothing
changes, no throw escapes), but it is the wrong result for a valid artifact, the S6a S1 pattern
again. Fix, one line: compare `sub(from, last) < recipe.cooldown`, which cannot overflow for
`0 <= last <= from`. Alternatively, bound `cooldown` in the schema (for example ResourceInt's
maximum), with an invalid case.

**N1 (nit): Elixir `current/3` uses `div` and a strict match, TypeScript uses `Math.floor` and
loose reads.** `lib/loka/core/compose.ex:98-100`. Both agree on every state a path can produce,
where clock and `at` are at least 0. They differ on a negative clock: TS changes the value, while
Elixir faults `precondition_failed`, because `div` truncates toward zero. They also differ on a
stored row without `at`: Elixir raises MatchError out of `compose`, while TS faults. The other ops
never raise on a malformed row. `Integer.floor_div/2` would match the documented
`floor(time / 3600)`, and a non-raising read would match TS's fault.

**N2 (nit): the delta's `resource.adjust` `from`/`to` are plain `integer`**
(`protocol/delta.schema.json:451,454`). The PM ruled 32-bit bounds, and `by`, `difficulty`,
`amount` and the spec fields all use `ResourceInt`. Compose bounds `to` and `from` in effect,
so nothing is wrong today. Tightening the schema after this freeze would be a breaking change,
though, and bounding it now costs two `$ref`s and one invalid case.

**N3 (nit): the branch predates main's #48** (the PM ruling's text, the 00 §4 amendment, and
the #48 line in `docs/reviews/README.md`). This record's README line is appended at the same
place, so merge main before merging the PR.

No blockers. Loader/compiler parity for every new field has red cases on both sides, and the
mutants above confirm them. The fixtures are hand-derived and verified. Both kernels agree on the
new ops.
