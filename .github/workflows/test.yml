name: Legacy Server Tests

on:
  push:
    branches: [main]
    paths:
      - "server/**"
      - ".github/workflows/test.yml"
  pull_request:
    branches: [main]
    paths:
      - "server/**"
      - ".github/workflows/test.yml"

jobs:
  test:
    name: Test (Elixir ${{matrix.elixir}} / OTP ${{matrix.otp}})
    runs-on: ubuntu-latest

    strategy:
      matrix:
        elixir: ['1.19.4']
        otp: ['28.3']

    env:
      MIX_ENV: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{matrix.elixir}}
          otp-version: ${{matrix.otp}}

      - name: Cache deps
        uses: actions/cache@v4
        with:
          path: |
            server/deps
            server/_build
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-

      - name: Install dependencies
        working-directory: ./server
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Check formatting
        working-directory: ./server
        run: mix format --check-formatted

      - name: Compile
        working-directory: ./server
        run: mix compile --warnings-as-errors

      # Content validation boots supervised application processes such as
      # EntitySeeder and Timers.Server. Run it in dev rather than the test SQL
      # sandbox, and explicitly prepare the validation database first.
      - name: Prepare content validation database
        working-directory: ./server
        env:
          MIX_ENV: dev
        run: |
          mix ecto.create --quiet
          mix ecto.migrate --quiet

      - name: Run content validation
        working-directory: ./server
        env:
          MIX_ENV: dev
        run: mix loka.test.validate --quiet

      - name: Run unit tests
        working-directory: ./server
        run: mix test --exclude full_storyline

      # The historical full-storyline bot suite is intentionally non-gating.
      # It currently depends on legacy world/storyline fixtures (for example
      # grove_arc) that are not part of the clean-sheet v3 rebuild. Keep those
      # tests available for manual/reference runs rather than blocking v3 work.
