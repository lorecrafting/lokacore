// Portable rules kernel, TypeScript side of candidate C (proposed ADR-071). Pure: no I/O,
// clock, randomness or Node APIs (lint/rules/ts-kernel-pure.yml). Rules arrive in R5.
export const KERNEL_ID = 'loka-kernel';
